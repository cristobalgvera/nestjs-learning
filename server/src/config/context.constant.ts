export enum GLOBAL {
  MAIN = 'Main',
}

export enum USER {
  REPOSITORY = 'UserRepository',
  SERVICE = 'UserService',
}

export enum AUTH {
  SERVICE = 'AuthService',
}

export enum JWT {
  STRATEGY = 'JwtStrategy',
}
